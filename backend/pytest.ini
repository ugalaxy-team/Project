[pytest]
strict_markers = true
markers = 
    slow: marks tests as slow (deselect with '-m "not slow"')

addopts = -v
asyncio_mode = auto
testpaths = tests

python_files = tests.py test_*.py *_test.py